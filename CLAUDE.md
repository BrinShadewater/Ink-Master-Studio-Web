@AGENTS.md

<!-- Canonical content is AGENTS.md beside this file (import since 2026-08-06);
     edit there, agent-neutral. -->

## Claude Code addendum

The four contracts above (key never in the browser, printify specs, deterministic
presets, local-first) are the constraints that matter; `npm run verify` is the gate
before claiming any change works.
